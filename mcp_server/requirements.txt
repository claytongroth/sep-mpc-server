# MCP Server framework
mcp>=1.0.0

# Vector database and embeddings
chromadb>=0.4.0
sentence-transformers>=2.2.2

# HTML processing
beautifulsoup4>=4.12.0

# Web scraping (for the original scraper)
requests>=2.31.0

# Data processing (NumPy 1.x installed separately for compatibility)